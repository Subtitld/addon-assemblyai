"""Subtitld AssemblyAI (Subtitld Cloud) add-on.

The package exists so the entry-point can be invoked both directly
(`python -m assemblyai_addon`, useful for development) and as a frozen
PyInstaller binary (`assemblyai-addon/assemblyai-addon`).
"""

__version__ = '1.0.1'
ADDON_ID = 'assemblyai'
PROTOCOL_VERSION = 1
